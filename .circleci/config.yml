version: 2.1
jobs:
  build:
    docker:
      - image: circleci/rust
    steps:
      - checkout
      - run:
        - name: check Rust version
        - command: rustup --version
      - run: cargo fmt -- --check
      - run: cargo test
